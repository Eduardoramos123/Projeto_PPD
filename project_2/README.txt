To run this project you need to:
Compile The Client script and Game_Server script with this commands in the /src of the project:

javac Client.java
javac Game_Server.java

Now you run the server in a shell by running thus command:

java Game_Server

And to run the client you have to open a new shell and run the command:

java Client

With this steps you can successfully run our project